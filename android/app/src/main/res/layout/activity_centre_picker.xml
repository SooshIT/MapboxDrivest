<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/dim_20">

    <ImageView
        android:id="@+id/centreLogo"
        android:layout_width="@dimen/logo_width_screen"
        android:layout_height="@dimen/logo_height_screen"
        android:contentDescription="@string/drivest_logo_content_description"
        android:src="@drawable/drivest_logo_tight"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/centrePickerTitle"
        android:layout_width="@dimen/dim_0"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/dim_8"
        android:text="@string/centre_picker_title"
        android:textColor="@color/ui_hex_231F20"
        android:textSize="@dimen/text_26"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/centreLogo" />

    <TextView
        android:id="@+id/centrePickerSubtitle"
        android:layout_width="@dimen/dim_0"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/dim_6"
        android:text="@string/centre_picker_subtitle"
        android:textColor="@color/ui_hex_5A5456"
        android:textSize="@dimen/text_14"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/centrePickerTitle" />

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/centreSearchLayout"
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
        android:layout_width="@dimen/dim_0"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/dim_16"
        android:hint="@string/centre_picker_search_hint"
        app:endIconMode="clear_text"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/centrePickerSubtitle"
        app:startIconDrawable="@android:drawable/ic_menu_search">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/centreSearchInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:maxLines="1"
            android:textColor="@color/ui_hex_231F20"
            android:textColorHint="@color/ui_hex_7A7376"
            android:textSize="@dimen/text_16" />
    </com.google.android.material.textfield.TextInputLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/centreRecyclerView"
        android:layout_width="@dimen/dim_0"
        android:layout_height="@dimen/dim_0"
        android:layout_marginTop="@dimen/dim_12"
        android:clipToPadding="false"
        android:paddingBottom="@dimen/dim_16"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/centreSearchLayout" />

    <TextView
        android:id="@+id/emptyCentresView"
        android:layout_width="@dimen/dim_0"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/centre_picker_empty"
        android:textColor="@color/ui_hex_7A7376"
        android:textSize="@dimen/text_15"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/centreRecyclerView"
        app:layout_constraintEnd_toEndOf="@id/centreRecyclerView"
        app:layout_constraintStart_toStartOf="@id/centreRecyclerView"
        app:layout_constraintTop_toTopOf="@id/centreRecyclerView" />

</androidx.constraintlayout.widget.ConstraintLayout>
